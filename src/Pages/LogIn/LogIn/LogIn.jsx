import { Helmet } from "react-helmet";

const LogIn = () => {
    return (
        <div>
            <Helmet>
                <title>Dish Hub | LogIn</title>
            </Helmet>
            <h1>Login</h1>
        </div>
    );
};

export default LogIn;